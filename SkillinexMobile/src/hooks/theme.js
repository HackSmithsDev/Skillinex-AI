export const COLORS = {
  // Primary Action Color (from your --primary: oklch(0.205 0 0))
  primary: '#0F172A', 
  
  // Success/Accent (Used sparingly now, matching your web --accent)
  accent: '#F1F5F9', 
  
  // Backgrounds
  bg_light: '#FFFFFF',
  bg_dark: '#020617', // Deeper slate for dark mode/terminals
  
  // Typography
  text_main: '#020617',
  text_muted: '#64748B', // oklch(0.556 0 0)
  
  // Borders & Dividers
  border: '#E2E8F0', // oklch(0.922 0 0)
  ring: '#94A3B8',
  
  // Error (oklch(0.577 0.245 27.325))
  error: '#7F1D1D', 
};

export const SIZES = {
  // Matching your web --radius: 0.5rem (8px)
  radius_sm: 4,
  radius_md: 8, 
  radius_lg: 12, 
  padding: 20,
};

export const FONTS = {
  // If you aren't using custom font loading, 
  // standard 'Inter' or System fonts work best
  black: 'Inter-Black',
  bold: 'Inter-Bold',
  italic: 'Inter-Italic',
};